---
// Preview page - 삭제 예정 (리디자인 확인용)
export const prerender = false;
---

<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Prints Section - Redesign Preview</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600&family=Zen+Kaku+Gothic+New:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="preview-banner">
      <strong>Preview</strong> &mdash; PrintsShowcase Redesign (5:5 split)
    </div>

    <div class="page-frame">
      <section class="section">
        <div class="container">
          <!-- Section Header -->
          <div class="section-header">
            <div class="section-title-group">
              <h2 class="section-title">Products</h2>
              <span class="section-subtitle">인쇄 상품</span>
            </div>
            <a href="/prints/" class="section-link">
              View all products
              <svg
                width="16"
                height="16"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <line x1="7" y1="17" x2="17" y2="7"></line>
                <polyline points="7 7 17 7 17 17"></polyline>
              </svg>
            </a>
          </div>

          <!-- 5:5 Split -->
          <div class="prints-split">
            <!-- Left: Featured Blog/Print Post -->
            <a href="/prints/1" class="featured-post">
              <div class="featured-image">
                <img
                  src="https://images.unsplash.com/photo-1586075010923-2dd4570fb338?w=800&h=1000&fit=crop&q=80"
                  alt="A4 리플렛 인쇄"
                  width="800"
                  height="1000"
                  loading="lazy"
                />
              </div>
              <div class="featured-content">
                <span class="featured-tag">Featured</span>
                <h3 class="featured-title">
                  고급 리플렛으로 브랜드 가치를 높이는 방법
                </h3>
                <p class="featured-excerpt">
                  인쇄물은 단순한 정보 전달 수단이 아닙니다. 브랜드의 첫인상을
                  결정짓는 중요한 요소입니다. 고급 용지 선택부터 후가공까지,
                  리플렛 인쇄의 모든 것을 알아보세요.
                </p>
                <time class="featured-date">2026.02.10</time>
              </div>
            </a>

            <!-- Right: Stacked Product Cards -->
            <div class="products-stack">
              <a href="/prints/2" class="stack-card">
                <div class="stack-card-image">
                  <img
                    src="https://images.unsplash.com/photo-1611532736597-de2d4265fba3?w=800&h=450&fit=crop&q=80"
                    alt="무선제본"
                    width="800"
                    height="450"
                    loading="lazy"
                  />
                </div>
                <div class="stack-card-content">
                  <div class="stack-card-text">
                    <h3 class="stack-card-title">무선제본 교재 인쇄</h3>
                    <p class="stack-card-sub">학원 교재, 사내 매뉴얼</p>
                  </div>
                  <span class="stack-card-tag">Binding</span>
                </div>
              </a>

              <a href="/prints/3" class="stack-card">
                <div class="stack-card-image">
                  <img
                    src="https://images.unsplash.com/photo-1588666309990-d68f235f7089?w=800&h=450&fit=crop&q=80"
                    alt="중철제본"
                    width="800"
                    height="450"
                    loading="lazy"
                  />
                </div>
                <div class="stack-card-content">
                  <div class="stack-card-text">
                    <h3 class="stack-card-title">중철 소책자 인쇄</h3>
                    <p class="stack-card-sub">카탈로그, 브로슈어, 메뉴판</p>
                  </div>
                  <span class="stack-card-tag">Booklet</span>
                </div>
              </a>

              <a href="/prints/4" class="stack-card">
                <div class="stack-card-image">
                  <img
                    src="https://images.unsplash.com/photo-1531538606174-e1ac68e3160c?w=800&h=450&fit=crop&q=80"
                    alt="전단지"
                    width="800"
                    height="450"
                    loading="lazy"
                  />
                </div>
                <div class="stack-card-content">
                  <div class="stack-card-text">
                    <h3 class="stack-card-title">전단지 &amp; 엽서 인쇄</h3>
                    <p class="stack-card-sub">이벤트 홍보, DM 발송</p>
                  </div>
                  <span class="stack-card-tag">Flyer</span>
                </div>
              </a>
            </div>
          </div>
        </div>
      </section>
    </div>
  </body>
</html>

<style is:global>
  /* ===== Design Tokens ===== */
  :root {
    --c-primary: #222828;
    --c-bg: #ffffff;
    --c-bg-secondary: #f5f7f7;
    --c-text: #222828;
    --c-text-sub: #5a6262;
    --c-text-light: #8a9292;
    --c-border-light: #f0f2f2;

    --sp-xs: 4px;
    --sp-sm: 8px;
    --sp-md: 16px;
    --sp-lg: 24px;
    --sp-xl: 32px;
    --sp-2xl: 48px;
    --sp-3xl: 64px;
    --sp-4xl: 96px;

    --fs-xs: 0.75rem;
    --fs-sm: 0.875rem;
    --fs-md: 1.125rem;
    --fs-xl: 1.5rem;
    --fs-2xl: 1.875rem;
    --fs-3xl: 2.25rem;

    --lh-tight: 1.25;
    --lh-snug: 1.375;
    --lh-normal: 1.5;

    --radius-xl: 0.75rem;
    --radius-full: 9999px;

    --transition-slow: 300ms;
    --transition-slower: 500ms;
    --ease-out-quart: cubic-bezier(0.25, 1, 0.5, 1);

    --font-family:
      "Outfit", "Zen Kaku Gothic New", -apple-system, BlinkMacSystemFont,
      sans-serif;
  }

  /* ===== Reset ===== */
  *,
  *::before,
  *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }
  body {
    font-family: var(--font-family);
    color: var(--c-text);
    background: var(--c-bg);
    -webkit-font-smoothing: antialiased;
  }
  a {
    text-decoration: none;
    color: inherit;
  }
  img {
    display: block;
    max-width: 100%;
    height: auto;
  }
</style>

<style lang="scss">
  .preview-banner {
    background: var(--c-primary);
    color: white;
    text-align: center;
    padding: var(--sp-md);
    font-size: var(--fs-sm);
    letter-spacing: 0.02em;

    strong {
      font-weight: 600;
    }
  }

  .page-frame {
    min-height: 100vh;
  }

  .section {
    padding: var(--sp-3xl) 0;
    @media (min-width: 900px) {
      padding: var(--sp-4xl) 0;
    }
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--sp-lg);
  }

  /* Section Header */
  .section-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--sp-3xl);
  }

  .section-title-group {
    display: flex;
    flex-direction: column;
    gap: var(--sp-xs);
  }

  .section-title {
    font-size: var(--fs-3xl);
    font-weight: 500;
    letter-spacing: -0.02em;
    line-height: var(--lh-tight);
  }

  .section-subtitle {
    font-size: var(--fs-sm);
    color: var(--c-text-light);
    font-weight: 400;
  }

  .section-link {
    display: inline-flex;
    align-items: center;
    gap: var(--sp-sm);
    font-size: var(--fs-sm);
    color: var(--c-text-sub);
    transition: color var(--transition-slow);

    &:hover {
      color: var(--c-text);
    }

    svg {
      transition: transform var(--transition-slow);
    }
    &:hover svg {
      transform: rotate(45deg);
    }
  }

  /* 5:5 Split */
  .prints-split {
    display: flex;
    flex-direction: column;
    gap: var(--sp-2xl);

    @media (min-width: 900px) {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: var(--sp-xl);
      align-items: start;
    }
  }

  /* Left: Featured */
  .featured-post {
    display: block;
    transition: transform var(--transition-slow) var(--ease-out-quart);
    &:hover {
      transform: translateY(-2px);
    }
  }

  .featured-image {
    width: 100%;
    overflow: hidden;
    border-radius: var(--radius-xl);

    img {
      width: 100%;
      aspect-ratio: 4 / 5;
      object-fit: cover;
      transition: transform var(--transition-slower) var(--ease-out-quart);
    }

    .featured-post:hover & img {
      transform: scale(1.03);
    }
  }

  .featured-content {
    margin-top: var(--sp-lg);
  }

  .featured-tag {
    display: inline-block;
    padding: var(--sp-xs) var(--sp-md);
    background: var(--c-bg-secondary);
    color: var(--c-text-sub);
    font-size: var(--fs-xs);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    border-radius: var(--radius-full);
    margin-bottom: var(--sp-md);
  }

  .featured-title {
    font-size: var(--fs-xl);
    font-weight: 500;
    line-height: var(--lh-snug);
    margin-bottom: var(--sp-sm);

    @media (min-width: 900px) {
      font-size: var(--fs-2xl);
    }
  }

  .featured-excerpt {
    font-size: var(--fs-sm);
    color: var(--c-text-sub);
    line-height: var(--lh-normal);
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .featured-date {
    margin-top: var(--sp-md);
    font-size: var(--fs-xs);
    color: var(--c-text-light);
  }

  /* Right: Stacked Cards */
  .products-stack {
    display: flex;
    flex-direction: column;
    gap: var(--sp-xl);

    .stack-card + .stack-card {
      padding-top: var(--sp-xl);
      border-top: 1px solid var(--c-border-light);
    }
  }

  .stack-card {
    display: block;
    transition: transform var(--transition-slow) var(--ease-out-quart);
    &:hover {
      transform: translateY(-2px);
    }
  }

  .stack-card-image {
    width: 100%;
    overflow: hidden;
    border-radius: var(--radius-xl);

    img {
      width: 100%;
      aspect-ratio: 16 / 9;
      object-fit: cover;
      transition: transform var(--transition-slower) var(--ease-out-quart);
    }

    .stack-card:hover & img {
      transform: scale(1.03);
    }
  }

  .stack-card-content {
    margin-top: var(--sp-md);
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: var(--sp-md);
  }

  .stack-card-text {
    flex: 1;
    min-width: 0;
  }

  .stack-card-title {
    font-size: var(--fs-md);
    font-weight: 500;
    line-height: var(--lh-snug);
  }

  .stack-card-sub {
    margin-top: var(--sp-xs);
    font-size: var(--fs-sm);
    color: var(--c-text-sub);
  }

  .stack-card-tag {
    flex-shrink: 0;
    padding: var(--sp-xs) var(--sp-md);
    background: var(--c-bg-secondary);
    color: var(--c-text-sub);
    font-size: var(--fs-xs);
    text-transform: uppercase;
    letter-spacing: 0.03em;
    border-radius: var(--radius-full);
    white-space: nowrap;
  }
</style>
