---
import Container from "@/components/layout/Container.astro";
import Section from "@/components/layout/Section.astro";
import SectionTitle from "@/components/ui/SectionTitle.astro";

interface Props {
  settings?: Record<string, string>;
}

const { settings = {} } = Astro.props;

// 섹션 제목
const sectionTitle = settings["about_values_title"] || "Our Values";
const sectionSubtitle =
  settings["about_values_subtitle"] || "Sungjinprint를 선택하는 이유";

// 4개 항목 데이터
const values = [
  {
    title: settings["about_values_item1_title"] || "품질 우선",
    description:
      settings["about_values_item1_content"] ||
      "최고 품질의 인쇄물을 제공하기 위해 최신 장비와 숙련된 기술력으로 고객의 기대를 뛰어넘는 결과물을 만들어냅니다.",
  },
  {
    title: settings["about_values_item2_title"] || "고객 중심",
    description:
      settings["about_values_item2_content"] ||
      "고객의 요구사항을 정확히 파악하고, 맞춤형 솔루션을 제공하여 고객 만족을 최우선으로 생각합니다.",
  },
  {
    title: settings["about_values_item3_title"] || "신속 정확",
    description:
      settings["about_values_item3_content"] ||
      "빠른 납기와 정확한 작업으로 고객의 소중한 시간을 절약해 드립니다.",
  },
  {
    title: settings["about_values_item4_title"] || "친환경 인쇄",
    description:
      settings["about_values_item4_content"] ||
      "환경을 생각하는 친환경 인쇄 공정과 재료를 사용하여 지속 가능한 인쇄 서비스를 제공합니다.",
  },
];
---

<Section>
  <Container>
    <div class="values-inner">
      <div class="values-header">
        <SectionTitle title={sectionTitle} subtitle={sectionSubtitle} />
      </div>

      <div class="values-list">
        {
          values.map((value) => (
            <article class="value-item">
              <h3 class="item-title">{value.title}</h3>
              <p class="item-description">{value.description}</p>
            </article>
          ))
        }
      </div>
    </div>
  </Container>
</Section>

<style lang="scss">
  .values-inner {
    @media (min-width: 900px) {
      display: grid;
      grid-template-columns: 20% 1fr;
      gap: 0 var(--sp-4xl);
    }
  }

  .values-header {
    display: flex;
    flex-direction: column;
    gap: var(--sp-md);
    margin-bottom: var(--sp-2xl);

    @media (min-width: 900px) {
      gap: var(--sp-lg);
      position: sticky;
      top: var(--sp-4xl);
      flex-direction: column;
      align-items: start;
      align-self: flex-start;
      flex-grow: 1;
      margin: 0;
      min-width: fit-content;
    }
  }

  .values-catchphrase {
    font-size: var(--fs-sm);
    line-height: var(--lh-loose);

    @media (min-width: 900px) {
      font-size: var(--fs-base);
    }
  }

  .values-list {
    display: flex;
    flex-direction: column;
    gap: var(--sp-2xl);

    @media (min-width: 600px) {
      gap: var(--sp-3xl);
      max-width: var(--container-sm);
    }
  }

  .value-item {
    display: flex;
    flex-direction: column;
    gap: var(--sp-md);

    @media (min-width: 900px) {
      gap: var(--sp-lg);
    }
  }

  .item-title {
    color: var(--c-text);
    font-size: var(--fs-lg);
    font-weight: 500;
    line-height: var(--lh-snug);

    @media (min-width: 900px) {
      font-size: var(--fs-xl);
    }
  }

  .item-description {
    color: var(--c-text-sub);
    font-size: var(--fs-sm);
    line-height: var(--lh-loose);

    @media (min-width: 900px) {
      font-size: var(--fs-base);
    }
  }
</style>
