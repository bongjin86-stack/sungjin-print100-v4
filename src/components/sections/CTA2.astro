---
import Container from "@/components/layout/Container.astro";
import Section from "@/components/layout/Section.astro";
import LinkButton from "@/components/ui/LinkButton.astro";
---

<Section background="secondary">
  <Container>
    <div class="cta2-grid">
      <div class="cta2-card">
        <h2 class="cta2-card-title">인쇄가이드</h2>
        <p class="cta2-card-description">
          용지 선택부터 후가공까지, 인쇄에 필요한 다양한 정보를 안내합니다.
        </p>
        <LinkButton href="/works" variant="solid" size="medium">
          가이드 보기
        </LinkButton>
      </div>

      <div class="cta2-card">
        <h2 class="cta2-card-title">서비스 안내</h2>
        <p class="cta2-card-description">
          무선제본, 중철제본, 스프링제본 등 다양한 제본 서비스를 제공합니다.
        </p>
        <LinkButton href="/services" variant="solid" size="medium">
          서비스 보기
        </LinkButton>
      </div>
    </div>
  </Container>
</Section>

<style lang="scss">
  .cta2-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--sp-xl);

    @media (min-width: 900px) {
      grid-template-columns: 1fr 1fr;
      gap: var(--sp-2xl);
    }
  }

  .cta2-card {
    display: flex;
    flex-direction: column;
    gap: var(--sp-lg);
    padding: var(--sp-2xl);
    border-radius: var(--radius-3xl);
    background-color: var(--c-bg);
    border: 1px solid var(--c-border-light);

    @media (min-width: 900px) {
      padding: var(--sp-3xl);
      gap: var(--sp-xl);
    }
  }

  .cta2-card-title {
    color: var(--c-text);
    font-size: var(--fs-xl);
    font-weight: 500;
    line-height: var(--lh-tight);
    letter-spacing: 0.02em;

    @media (min-width: 900px) {
      font-size: var(--fs-2xl);
    }
  }

  .cta2-card-description {
    flex: 1;
    color: var(--c-text-sub);
    font-size: var(--fs-sm);
    line-height: var(--lh-loose);

    @media (min-width: 900px) {
      font-size: var(--fs-base);
    }
  }
</style>
