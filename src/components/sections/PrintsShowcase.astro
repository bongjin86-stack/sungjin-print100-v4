---
import Container from "@/components/layout/Container.astro";
import Section from "@/components/layout/Section.astro";
import LinkText from "@/components/ui/LinkText.astro";
import SectionTitle from "@/components/ui/SectionTitle.astro";

import { supabase } from "../../lib/supabase";

// 랜딩 페이지 설정에서 상품 설정 가져오기
const { data: settings } = await supabase
  .from("site_settings")
  .select("key, value")
  .in("key", [
    "landing_products_title",
    "landing_products_subtitle",
    "landing_products_link_text",
    "landing_products_description",
    "landing_products_image",
    "landing_products_sub_items",
  ]);

const configMap: Record<string, string> = {};
(settings || []).forEach((s: { key: string; value: string }) => {
  configMap[s.key] = s.value;
});

const sectionTitle = configMap.landing_products_title || "Products";
const sectionSubtitle = configMap.landing_products_subtitle || "인쇄 상품";
const linkText = configMap.landing_products_link_text || "View all products";
const description = configMap.landing_products_description || "";
const mainImage = configMap.landing_products_image || "";

// 서브 아이템 파싱
interface SubItem {
  type: "custom" | "product";
  image?: string;
  title?: string;
  text?: string;
  productId?: string;
}

let subItems: SubItem[] = [];
try {
  subItems = JSON.parse(configMap.landing_products_sub_items || "[]");
} catch {
  subItems = [];
}

// product 타입 서브 아이템의 상품 데이터 가져오기
const productIds = subItems
  .filter((item) => item.type === "product" && item.productId)
  .map((item) => item.productId!);

let productsMap: Record<string, any> = {};
if (productIds.length > 0) {
  const { data } = await supabase
    .from("prints")
    .select("id, title, image, tag")
    .in("id", productIds)
    .eq("is_published", true);

  if (data) {
    data.forEach((p: any) => {
      productsMap[p.id] = p;
    });
  }
}

// 렌더용 서브 아이템 조합
const resolvedSubs = subItems
  .map((item) => {
    if (item.type === "product" && item.productId) {
      const p = productsMap[item.productId];
      if (!p) return null;
      return { image: p.image, title: p.title, text: p.tag || "", href: `/prints/${p.id}` };
    }
    return { image: item.image || "", title: item.title || "", text: item.text || "", href: "" };
  })
  .filter(Boolean) as { image: string; title: string; text: string; href: string }[];
---

<Section background="primary">
  <Container>
    <div class="prints-showcase-inner">
      <div class="prints-showcase-header">
        <SectionTitle title={sectionTitle} subtitle={sectionSubtitle} />
        {description && (
          <p class="prints-showcase-desc">{description}</p>
        )}
        <LinkText href="/prints/" ariaLabel="전체 상품 보기">
          {linkText}
        </LinkText>
      </div>

      <div class="prints-library-content">
        <div class="prints-library-main">
          {mainImage ? (
            <img
              src={mainImage}
              alt={sectionTitle}
              width="960"
              height="720"
              loading="lazy"
            />
          ) : (
            <div class="prints-library-placeholder">No Image</div>
          )}
        </div>

        {resolvedSubs.length > 0 && (
          <div class="prints-library-subs">
            {resolvedSubs.map((sub) => {
              const inner = (
                <div class="prints-library-sub">
                  {sub.image && (
                    <div class="prints-library-sub-image">
                      <img
                        src={sub.image}
                        alt={sub.title || ""}
                        width="320"
                        height="240"
                        loading="lazy"
                      />
                    </div>
                  )}
                  {(sub.title || sub.text) && (
                    <div class="prints-library-sub-text">
                      {sub.title && <h3 class="prints-library-sub-title">{sub.title}</h3>}
                      {sub.text && <p class="prints-library-sub-desc">{sub.text}</p>}
                    </div>
                  )}
                </div>
              );
              return sub.href ? <a href={sub.href} class="prints-library-sub-link">{inner}</a> : inner;
            })}
          </div>
        )}
      </div>
    </div>
  </Container>
</Section>

<style lang="scss">
  .prints-showcase-inner {
    @media (min-width: 900px) {
      display: grid;
      grid-template-columns: 20% 1fr;
      gap: 0 var(--sp-4xl);
    }
  }

  .prints-showcase-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--sp-lg);
    margin-bottom: var(--sp-3xl);

    @media (min-width: 900px) {
      position: sticky;
      top: var(--sp-4xl);
      flex-direction: column;
      align-items: start;
      align-self: flex-start;
      flex-grow: 1;
      margin: 0;
      min-width: fit-content;
    }
  }

  .prints-showcase-desc {
    display: none;

    @media (min-width: 900px) {
      display: block;
      color: var(--c-text-sub);
      font-size: var(--fs-sm);
      line-height: var(--lh-relaxed);
      margin: 0;
    }
  }

  .prints-library-content {
    display: flex;
    flex-direction: column;
    gap: var(--sp-xl);
  }

  .prints-library-main {
    width: 100%;
    overflow: hidden;
    border-radius: var(--radius-3xl);

    img {
      width: 100%;
      height: auto;
      object-fit: cover;
      aspect-ratio: 5 / 6;
      display: block;

      @media (min-width: 900px) {
        aspect-ratio: 2.3 / 1;
      }
    }
  }

  .prints-library-placeholder {
    width: 100%;
    aspect-ratio: 5 / 6;

    @media (min-width: 900px) {
      aspect-ratio: 2.3 / 1;
    }
    background-color: var(--c-bg-secondary);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--c-text-sub);
  }

  .prints-library-subs {
    display: flex;
    flex-direction: column;
    gap: var(--sp-lg);
  }

  .prints-library-sub-link {
    display: block;
    text-decoration: none;
    color: inherit;
    transition: opacity var(--transition-base);

    &:hover {
      opacity: 0.8;
    }
  }

  .prints-library-sub {
    display: flex;
    gap: var(--sp-lg);
    align-items: start;

    @media (min-width: 600px) {
      gap: var(--sp-xl);
    }
  }

  .prints-library-sub-image {
    flex: 0 0 36%;
    overflow: hidden;
    border-radius: var(--radius-2xl);

    @media (min-width: 600px) {
      flex: 0 0 30%;
    }

    @media (min-width: 900px) {
      flex: 0 0 28%;
    }

    img {
      width: 100%;
      height: auto;
      object-fit: cover;
      aspect-ratio: 4 / 3;
      display: block;
    }
  }

  .prints-library-sub-text {
    flex: 1;
    min-width: 0;
    padding-top: var(--sp-xs);
  }

  .prints-library-sub-title {
    color: var(--c-text);
    font-size: var(--fs-base);
    font-weight: 600;
    line-height: var(--lh-snug);
    margin: 0 0 var(--sp-xs);

    @media (min-width: 600px) {
      font-size: var(--fs-lg);
    }
  }

  .prints-library-sub-desc {
    color: var(--c-text-sub);
    font-size: var(--fs-sm);
    line-height: var(--lh-relaxed);
    margin: 0;

    @media (min-width: 600px) {
      font-size: var(--fs-base);
    }
  }
</style>
